// Display-only. eve resolves the real model from static source in
// agent/agent.ts, so this must be kept in sync with it by hand.
export const DEFAULT_MODEL_ID = "gpt-5.4";
export const MODEL_LABEL = "GPT-5.4";
